PORT=4000
MONGO_URI=mongodb+srv://<user>:<password>@<cluster>.mongodb.net/?appName=BalaghJo
JWT_SECRET=replace_with_a_long_random_string
JWT_EXPIRES_IN=30m
UPLOAD_DIR=uploads
MAX_UPLOAD_MB=10
CORS_ORIGINS=
# Optional: absolute path to a Firebase service-account JSON for FCM push
# notifications. When unset, the API runs with push notifications disabled.
FCM_SERVICE_ACCOUNT_PATH=
